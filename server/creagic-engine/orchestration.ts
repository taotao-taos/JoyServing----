/**
 * Creagic 驾驭工程：编排智能体行为层（推理习惯、澄清策略、输出结构等）。
 * 修改本文件即可迭代「更聪明」的策略，无需动 aiHandlers 主流程。
 */

/** 仅在有「多段编排 prior + 终稿」且命中分镜/视频类意图时注入，见 aiHandlers */
/** 终稿补充：少套话，多交付；可与前端自动关键帧衔接 */
export const STORYBOARD_FINAL_REPLY_RULES = `
【分镜/视频类 · 终稿】
- 正文以可直接执行的镜头与画面描述为主，少写「意图/流程」类元叙述。
- 若用户已要求出图或描述已够具体：优先调用 generate_image（若可用）生成一格关键帧；否则给出一段可复制的生图提示词。
- 勿承诺「成片视频已生成」；长分镜可拆成多轮，每次聚焦少量镜头。
`.trim();
